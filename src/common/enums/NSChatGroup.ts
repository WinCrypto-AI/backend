export namespace NSChatGroup {
  export enum ERole {
    MEMBER = 'MEMBER',
    ADMIN = 'ADMIN',
  }
  export enum EType {
    GLOBAL = 'GLOBAL',
    VN = 'VN',
  }
  export enum EMode {
    GROUP = 'GROUP', // Dạng nhóm
    CHANNEL = 'CHANNEL', // Dạng kênh
  }
}
